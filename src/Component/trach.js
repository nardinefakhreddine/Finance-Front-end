  /* constructor() {
     super();
     this.state = {
       email: "",
       password: "",
     }
   }
   //Fetch API here
   /* componentDidMount(){
      fetch('http://localhost:8000/api/test').then(function(response){
        response.json().then(function(resp){
          console.log(resp['name']);
    })
  
      });
    }*/
  // submit() {
  /* const requestOptions = {
     method: 'POST',
     headers: {
       'Accept': 'application/json',
       'Content-Type': 'application/json',
       //'Authorization': 'Bearer my-token',
       // 'My-Custom-Header': 'foobar'
     },
     body: JSON.stringify(this.state)
   };*/

  /* fetch('http://localhost:8000/api/test').then(function (response) {
     response.json().then(function (resp) {
       console.log(resp['name']);
     })

   });


   /* fetch('http://localhost:8000/api/test', requestOptions)
        .then(function(response){
          response.json().then(function(resp){
            console.log(resp);
          })
        })*/


  //}//FIN SUBMIT

  /* <div className="App" >
          <h1> test</h1>
          <input type="text" name="email" onChange={(user) => { this.setState({ email: user.target.value }) }} />
          <input type="password" name="password" onChange={(user) => { this.setState({ password: user.target.value }) }} />
          <button onClick={() => { this.submit() }}> LOgin</button>
        </div>*/

  //fin submit